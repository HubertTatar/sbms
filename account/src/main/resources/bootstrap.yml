spring:
  application:
    name: account
  cloud:
    config:
      discovery:
        enabled: true
        service-id: config-server
    bus:
      trace:
        enabled: true
  profiles:
    active: dev
eureka:
  client:
    serviceUrl:
      defaultZone:  http://localhost:8000/eureka/